"use client";

import { ReservationCard } from "./ReservationCard";
import { ReservationContainer } from "./ReservationContainer";

export const LeadReservation = () => {
    return (
        <ReservationContainer>
            <ReservationCard />
        </ReservationContainer>
    );
};
